# Frontend – Local development
# Copy to .env.local and adjust as needed

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000

# Current user ID for API calls (no auth yet)
# Must match a seeded user. Run backend seed first; dev user has id user-001
VITE_CURRENT_USER_ID=user-001
