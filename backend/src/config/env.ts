// src/config/env.ts
// Loads and provides typed access to environment variables
// TODO: Implement environment variable validation and access

export const ENV = {
  // Example: PORT: process.env.PORT || '3000',
};
